---
apiVersion: v1
kind: Service
metadata:
  name: my-dashboard
spec:
  type: {{ .Values.service.type }}
  selector:
    app: my-dashboard
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      nodePort: {{ .Values.service.nodePort }}
