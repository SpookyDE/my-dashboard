---
version: "3.9"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data.json:/app/data.json
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
